server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Listado de directorios habilitado para navegar entre ejercicios
    location / {
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
        try_files $uri $uri/ =404;
    }
}
